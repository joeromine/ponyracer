import { SimpleTExtDirectiveDirective } from './simple-text-directive.directive';

describe('SimpleTExtDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleTExtDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
